pub fn audioClipPipeline(
    clip: &AudioClip,
    sample_rate: u32,
    sample_index: u32,
) -> Result<Vec<i16>, String> {
    if (clip.file) {
        if (clip.)
    } else {
        return Err("No file".to_string());
    }
}